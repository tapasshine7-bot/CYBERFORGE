# Preview Host Diagnosis

The temporary sandbox browser reached the Vite preview on 2026-08-21, but Vite rejected the proxied `*.manus.computer` hostname. This is a local development-preview concern only and does not affect the production build. The Vite development configuration now explicitly permits the current preview hostname for visual QA.

## Visual Verification

The overview rendered successfully in the sandbox browser after the preview hostname was allowed. The desktop view showed the intended containment-console composition: persistent navigation rail, readable dark graphite interface, high-contrast Forge Lime signals, overview metrics, active-practice preview, and clear mobile-safe visual hierarchy. No clipping or white-flash issue was observed in the initial desktop viewport.

## User Preview Screens

The desktop Overview workspace was captured with its status panel, current practice card, milestone, and legal-practice principle.

The Practice Terminal was captured with the simulated isolated environment notice, local command prompt, and guided learning-safe commands. The screen visibly states that outbound access is blocked.

The Learning Paths screen was captured with four curricula cards: Linux Foundations, Defensive Web Security, Incident Response Basics, and Authorized Lab Method.

The Defensive Tools screen was captured with browser-local SHA-256, password-hygiene, and URL-format utilities. Its privacy notice is visibly present.

The Knowledge Base was captured with four lawful security-work topics and a prominent legal-use notice.

The Progress dashboard was captured with browser-local completion tracking and the readiness metric.
